

package com.mycompany.sistema.controllers;

/**
 * Interface para que cualquier clase que lo use implemente este método, y ais no solo definir una sola
 * @author juego
 */
public interface SidebarActions {
    void ocultarSidebar(); //solo ocultar, porque es el que se encuentra en el FXML inyectado
}

